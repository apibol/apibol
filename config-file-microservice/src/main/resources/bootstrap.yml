---
spring:
  application:
    name: config-file-microservice
  profiles:
    active: native

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/