---
spring:
  profiles: default
  application:
    name: predictors
  cloud:
    config:
      uri: http://localhost:8001/
---
spring:
  profiles: docker
  application:
    name: predictors
  cloud:
    config:
      uri: http://config:8001/
